# ============================================================
# LEM StratIQ — AI Robo-Advisor
# Python dependencies (pip install -r requirements.txt)
#
# Pinned to minimum verified versions.
# Run: pip install -r requirements.txt
# ============================================================

# ── Core Streamlit & UI ──────────────────────────────────────
streamlit>=1.40.0
plotly>=5.20.0
streamlit-oauth>=0.1.5
streamlit-cookies-controller>=0.0.4   # server-side session cookie bridge

# ── Data & ML ───────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0        # train_model.py: RandomForest, StandardScaler, LabelEncoder
yfinance>=0.2.38           # market_updater.py, ui/page_insights.py

# ── Database ────────────────────────────────────────────────
pymongo>=4.7.0
bcrypt>=4.1.0

# ── REST API (backend_api.py) ────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0            # request/response models in backend_api.py
httpx>=0.27.0              # async HTTP client used in tests + nav.py

# ── AI / LLM ────────────────────────────────────────────────
anthropic>=0.34.0          # Claude integration in app.py
google-generativeai>=0.7.0 # Gemini chatbot (server-side call in ui/chatbot.py)

# ── Auth & HTTP ─────────────────────────────────────────────
Authlib>=1.3.2
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
requests>=2.31.0

# ── Payments (optional — only needed if Stripe key configured) ─
stripe>=9.0.0              # ui/page_account.py page_billing() production path

# ── Testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0     # async test support for FastAPI integration tests
